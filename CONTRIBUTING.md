# 贡献指南

感谢你参与安归。当前项目处于需求和 MVP 设计阶段；贡献的首要目标是建立可信、安全、可演示的基础，而不是扩大未经验证的功能承诺。

## 开始之前

请先阅读：

- [README.md](./README.md)
- [docs/PRODUCT.md](./docs/PRODUCT.md)
- [docs/SECURITY_AND_PRIVACY.md](./docs/SECURITY_AND_PRIVACY.md)
- [CODE_OF_CONDUCT.md](./CODE_OF_CONDUCT.md)

如果改动涉及真实案件数据、定位、人脸/监控、权限绕过、自动派遣、对外发布或第三方敏感数据传输，请先停止实现并获得项目负责人和安全评审人员的明确批准。部分能力属于明确禁止范围，不能以“先做 Demo”为理由加入。

## 贡献类型

- 澄清需求、术语和验收标准；
- 改进模拟案件、演示脚本和无障碍体验；
- 实现三端原型、权限、审计、状态机和降级流程；
- 改进 AI 结构化输出、评估集和人工审核界面；
- 修复安全、隐私、可靠性和文档问题；
- 增加经授权、脱敏和审核的学习材料。

## 工作流程

1. 用 Issue 或任务说明记录问题、范围、风险和验收方式；若仓库尚未启用 Issue，则在变更说明中提供这些信息。
2. 保持改动小而聚焦，不混入无关重构或格式化。
3. 代码、测试、配置模板和文档同步更新。
4. 使用模拟数据测试；不要复制真实案件、群聊、照片或账号信息。
5. 提交前运行项目规定的格式检查、静态检查和测试。
6. 在评审说明中列出安全影响、AI 行为变化、第三方服务变化和未解决风险。

## Issue、分支与合并请求规则

### 一个 Issue 对应一个合并请求

- 每个需要修改代码、配置、迁移、测试或文档的 GitHub Issue，必须使用**独立的分支和独立的 Pull Request（PR）**完成。
- 一个 PR 只解决一个 Issue，并在 PR 描述中使用 `Closes #编号` 或 `Fixes #编号` 关联该 Issue。不要在同一个 PR 中顺带修改无关 Issue。
- 如果一个 Issue 的范围已经大到无法在一个可评审 PR 中完整交付，应先在 Issue 中拆出新的子 Issue 或后续 Issue；不要用一个“大而不完整”的 PR 混合交付多个目标。
- 纯讨论、澄清或无需仓库改动的 Issue 不需要 PR；一旦修改仓库内容，仍适用本节规则。

### 完整完成后才能关闭 Issue

- PR 合并或 Issue 关闭前，必须完成该 Issue 正文中的**全部范围、验收标准和明确依赖**。
- 不允许只完成一部分功能就关闭 Issue，也不允许把未完成验收项隐藏在“后续再说”中。
- 范围发生变化时，先更新 Issue 的范围、验收标准和风险说明；无法在当前 Issue 内完成的内容必须移入新的 Issue，并在原 Issue 中留下链接和原因。
- 对 API、权限、状态机、隐私、AI、地图或定位相关 Issue，OpenAPI、前端调用类型、服务端实现、测试和相关文档均属于同一次交付的一部分。
- Issue 的验证结果、已知限制和未完成依赖必须写回 Issue 或关联 PR，方便后续按 `#编号` 追踪。

### PR 必填说明

每个 PR 描述至少包含以下内容：

```md
Closes #<issue-number>

## 变更内容
- 本 PR 完成了什么

## 验收对应
- [ ] 已逐项完成 Issue 中的验收标准
- [ ] 已同步更新 API / 文档 / 配置（如适用）

## 测试与质量检查
- [ ] 新增或更新了与改动对应的自动化测试
- [ ] 列出实际执行的命令及结果
- [ ] 已检查代码格式、静态检查和项目既有代码风格

## 风险与边界
- 权限、隐私、AI、定位、第三方服务或已知限制的变化；无则写“无”。
```

## 测试与代码风格要求

- 每个涉及代码行为变化的 PR 必须新增或更新自动化测试；只修改文档时，也必须在 PR 的“测试与质量检查”中说明为何不需要运行行为测试，并完成链接、示例和 Markdown 检查。
- 测试必须与改动同一层级：新增/修改 HTTP 端点要有对应 API 或集成测试；服务逻辑要有单元或服务层测试；权限与字段裁剪要覆盖允许、拒绝和越权路径；前端交互改动要有可重复的验证步骤。
- 测试文件、测试命名、断言风格、错误处理和数据准备必须遵循仓库现有模式。不要为了通过测试引入 `unwrap()`、跳过错误路径、依赖本机私有数据，或绕开服务端权限检查。
- Rust 代码使用 `cargo fmt` 格式化，保持 Clippy 无警告；前端遵循既有 TypeScript、React、ESLint 和格式风格。不要在同一 PR 中混入无关格式化或大规模重排。
- 使用 `.invalid` 账号和虚构/脱敏数据。测试不得输出密码、令牌、真实位置、完整病史、原始聊天或未处理附件。
- 修复 Bug 时，先添加能复现问题的测试；修复后该测试必须通过，以防回归。

仓库使用 React/Vite 前端与 Rust/Actix Web 后端。提交前至少运行与改动相关的命令：

```powershell
npm run format:backend
npm run check:backend
npm run test:backend
npm run lint:frontend
npm run build:frontend
```

当前尚未确定长期分支策略、数据库和端到端测试工具；相关能力确定后，应在同一变更中补充准确命令和规范。

## 变更质量要求

### 通用要求

- 代码和界面使用一致的中文术语；必要时同时提供稳定英文标识。
- 对用户可见的错误给出可行动提示，不泄露堆栈、密钥或内部数据。
- 新增配置同时提供无敏感值的示例和说明。
- 新依赖说明用途、许可证、维护状态和安全影响。
- 不在日志、测试快照或示例中存放真实个人信息。

### 权限和数据改动

- 默认拒绝，显式授权；服务端执行检查。
- 增加越权和字段泄露测试。
- 明确数据级别、保留期、删除与导出方式。
- 数据库迁移应可审查，并说明回滚或兼容方案。

### AI 改动

- 说明任务输入、输出结构、允许和禁止行为。
- 保存模板/模型版本，结构化输出必须校验。
- 增加冲突信息、缺失信息、提示注入、越权提问和无依据回答测试。
- UI 必须区分 AI 草稿和人工确认结果。
- 不能只以少量顺利样例作为效果证明。

### 地图和定位改动

- 说明采集时机、频率、精度、可见角色和保留期。
- 提供停止回传和外部服务失败的降级路径。
- 测试无权限查看、任务结束、过期位置和坐标异常。

## 提交与评审说明

建议提交信息采用清晰的动词和范围，例如：

- `docs: clarify clue review states`
- `feat(auth): restrict volunteer case view`
- `test(ai): add conflicting-location cases`
- `fix(map): stop tracking after task completion`

合并请求或变更说明至少回答：

- 对应哪个 Issue 编号，是否完整满足其全部验收标准；
- 改了什么，为什么；
- 如何验证，新增/更新了哪些测试，实际执行了哪些质量命令；
- 是否改变权限、隐私、AI 输出、定位或分发行为；
- 使用了什么模拟数据；
- 有哪些已知限制和后续工作。

## 高风险改动

以下改动至少需要产品、技术和安全/隐私三个角度的评审：

- 身份认证、角色、案件授权和导出；
- 精确位置采集、轨迹回放和人员安全告警；
- 真实数据接入、数据出境或第三方 AI 调用；
- AI 自动改变线索状态、发布内容或创建任务；
- 文件上传、公开链接、通知和分享；
- 数据删除、备份、审计日志和密钥管理；
- 从演示环境升级为外部试用或正式部署。

## 文档维护

功能状态变化时必须更新相应文档：已经实现的能力应附验证方式；取消的能力应从 Demo 和路演材料删除；新增限制应同步到 README、安全文档、分发清单和 `CODEX.md`。
