# Use the sparse rsproxy mirror for Cargo downloads. Checksums remain verified
# against Cargo.lock, so this changes transport only, not dependency versions.
[source.crates-io]
replace-with = "rsproxy"

[source.rsproxy]
registry = "sparse+https://rsproxy.cn/index/"

[net]
git-fetch-with-cli = true
