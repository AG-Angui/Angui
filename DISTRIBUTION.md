# 分发与发布规范

## 1. 当前分发状态

安归目前仅有需求与设计文档，没有可发布的软件构建物，也没有开源许可证。除非版权所有者另行书面授权，默认保留全部权利：不得擅自公开镜像、安装包、源代码副本、数据包、模型提示、演示账号或以安归名义提供服务。

本文描述未来演示包、内部测试包和正式版本的最低分发要求，不代表已经批准任何公开发布。

## 2. 分发级别

| 级别 | 用途 | 数据与访问要求 | 批准要求 |
| --- | --- | --- | --- |
| D0 本地开发 | 开发者个人环境 | 仅模拟数据；本地开发密钥 | 模块负责人 |
| D1 内部演示 | 团队评审、录屏 | 固定脱敏数据；临时账号；受控访问 | 项目负责人 |
| D2 竞赛交付 | 评委体验、初赛材料 | 专用演示环境；最小权限；到期关闭 | 项目负责人 + 安全检查 |
| D3 限定试用 | 获批组织的小范围验证 | 单独协议、培训、审计和事件响应 | 法律/隐私/安全/业务共同批准 |
| D4 正式发布 | 真实业务或公开服务 | 生产级合规、安全、运维和持续支持 | 尚未批准 |

D1/D2 不得通过更换文案被包装成真实搜救系统。D3/D4 需要新的风险评估，不能沿用比赛 Demo 的默认配置。

## 3. 构建物范围

未来可能分发的内容包括：

- Web 前端静态构建物；
- 后端服务镜像或部署包；
- 数据库迁移；
- 去敏后的配置模板；
- 模拟数据初始化包；
- 演示账号初始化脚本；
- 用户/管理员操作说明；
- 版本说明、依赖清单和软件物料清单（SBOM）；
- 校验和或签名。

真实密钥、真实案件、原始聊天、完整轨迹、生产数据库副本、个人账号和内部安全报告永远不属于普通分发包。

## 4. 发布门槛

每次 D1 及以上分发前必须确认：

### 产品与声明

- 功能描述与当前实现一致，计划功能未标记为已完成。
- 明确“AI 辅助、人工复核”和“不能替代专业救援机构”。
- 演示环境明显标识模拟案件和虚构数据。
- 不宣称未经验证的找回率、预测准确率、官方合作或生产能力。

### 数据与隐私

- 对数据包、数据库、对象存储、日志、截图和视频完成隐私扫描。
- 演示照片和素材具有可证明的使用权。
- 账号、公开链接和定位数据设置到期时间。
- 导出、备份和删除流程已经验证。

### 安全

- 不包含硬编码密钥、调试后门、默认弱密码或开放管理端口。
- 完成依赖漏洞扫描、权限测试和基础 Web 安全检查。
- 密钥按环境隔离，演示结束后可撤销。
- 文件上传、AI 输入和分享功能具有限制与审计。
- 准备外部服务不可用和疑似数据泄露的处理方式。

### 工程质量

- 版本号、提交标识、构建时间和环境明确。
- 安装/部署/回滚说明经过另一位成员复现。
- 自动化测试和人工 Demo 流程通过。
- 发布包生成过程可重复，不依赖个人机器上的未记录文件。

### 法律与第三方

- 项目自身许可证或授权范围已经明确。
- 审查所有依赖、字体、图标、图片、地图和模型服务的许可/条款。
- 正确展示高德地图、阿里云/通义千问及其他服务要求的标识和归属。
- 不把第三方 API 密钥、数据或缓存内容作为可自由再分发资产。

## 5. 版本与发布记录

工程建立后建议采用语义化版本：

- `0.x.y`：原型和不稳定接口；
- `1.0.0`：只有达到明确的生产发布标准后使用；
- 预发布后缀：例如 `0.1.0-demo.1`、`0.2.0-rc.1`。

每个版本说明至少包含新增、变更、修复、安全与隐私影响、数据迁移、已知问题、验证结果和升级/回滚步骤。涉及敏感漏洞时，公开说明不得包含可直接利用的细节。

## 6. Demo 环境生命周期

1. 创建独立环境、独立账号和最小权限密钥。
2. 导入经过复核的固定模拟数据。
3. 发布前执行清单并记录批准人。
4. 演示期间监控异常登录、配额和上传内容。
5. 演示结束后撤销公开访问、账号和密钥。
6. 删除临时上传、运行日志中的非必要数据和临时备份。
7. 记录版本、问题与改进项，不保留不必要的个人输入。

## 7. 容器、镜像和托管平台

未来发布镜像时：

- 使用最小基础镜像和非 root 用户；
- 固定基础镜像与依赖版本；
- 不把 `.env`、密钥或数据文件复制进镜像层；
- 提供健康检查和资源限制建议；
- 扫描镜像漏洞并记录例外；
- 默认将仓库/镜像设为私有，公开前完成许可与安全审核。

使用第三方托管平台时，需确认部署区域、日志、备份、访问控制、数据删除和供应商条款。

## 8. 停止分发与召回

出现以下情况应暂停下载/访问并撤销相关凭据：

- 发现真实个人信息、未授权照片或可重新识别的案例数据；
- 存在可导致越权、位置泄露、账号接管或危险任务的漏洞；
- 第三方服务条款、许可或授权不允许当前分发方式；
- 演示内容造成官方合作、真实能力或 AI 确定性的重大误解；
- 版本无法安全升级、删除数据或停止定位。

召回后应记录受影响版本、渠道、接收方、处置措施和恢复条件。

## 9. 发布负责人待办

在首次对外分发前，项目负责人仍需补充：

- 软件许可证与版权声明；
- 正式仓库和制品库地址；
- 发布签名和校验方式；
- 安全联系渠道；
- 第三方依赖清单和归属文本；
- 数据处理/隐私告知文本；
- 各分发级别的具名审批人。
