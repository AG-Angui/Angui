# Codex 仓库协作说明

本文件指导 Codex 和其他自动化编码代理在安归仓库中工作。目标是让代理基于真实仓库状态推进项目，同时避免把计划能力误报为已实现，或引入与搜救、隐私和人员安全冲突的设计。

## 1. 项目现状

- 项目名称：安归。
- 场景：面向失智老人走失搜救的 AI + 地图协同。
- 当前阶段：需求拆分与 MVP 设计；尚未确定工程栈，尚无可运行应用。
- 产品需求：[docs/PRODUCT.md](./docs/PRODUCT.md)。
- 文档入口：[docs/README.md](./docs/README.md)。

除非仓库出现可运行代码、测试和验证记录，否则不得声称家属端、指挥端、志愿者端、AI、RAG、地图、轨迹或部署能力已经实现。

## 2. 开始任务时

1. 读取用户请求和与任务直接相关的文档。
2. 检查仓库结构、已有改动和适用的代理说明文件。
3. 确认请求属于文档、原型、实现、诊断还是发布工作，不擅自扩大范围。
4. 涉及权限、隐私、定位、真实数据、AI 决策或对外分发时，先阅读 `docs/SECURITY_AND_PRIVACY.md` 和 `DISTRIBUTION.md`。
5. 如果技术选型尚未决定，优先实现与框架无关的文档/接口约束，或明确列出假设；不要悄悄选择并宣称为项目既定方案。

## 3. 需求优先级

发生冲突时按以下顺序处理：

1. 用户在当前任务中的明确要求；
2. 人身安全、法律授权、隐私和最小权限边界；
3. 仓库根目录及更近目录中的代理说明；
4. `docs/PRODUCT.md` 和其他维护后的项目文档；
5. 仓库提交历史中可验证的既有决策；
6. 代理自行作出的便利性假设。

不得利用优先级解释来绕过强制安全边界。如果用户要求真实数据接入、监控/人脸识别、越权访问或无人化危险调度，应明确指出风险并停止相关实现。

## 4. 不可突破的规则

- 不接入或抓取未经授权的监控、公安、医疗、运营商、社交平台或其他敏感数据源。
- 不实现人脸识别、跨摄像头人员确认或隐蔽跟踪。
- 不让 AI 自动把猜测变成事实、公开通报或高风险任务。
- 不输出“老人一定在哪里”等确定性结论；建议必须可追溯、有不确定性并等待人工核实。
- 不向家属、公众或无关志愿者泄露精确位置、完整身份、电话、病史、原始聊天或内部策略。
- 不使用真实案件材料作为示例、测试夹具、截图、种子数据或模型评估输入。
- 不提交密钥、令牌、密码、生产 URL、个人账号或包含敏感值的 `.env`。
- 不把演示原型描述为可直接用于真实救援的生产系统。

## 5. 工程实现准则

工程骨架建立后，代理应：

- 以最小、可审查的改动完成请求，保留用户的现有修改。
- 先复用项目现有技术和模式，避免为局部需求引入第二套框架。
- 让权限检查发生在服务端；前端隐藏不构成安全控制。
- 对案件、线索、任务、轨迹和 AI 结果使用明确状态，不用模糊布尔值替代业务状态机。
- 对关键事实保留版本和审计信息，不静默覆盖人工确认结果。
- 对外部 AI、地图、存储服务设置超时、错误处理和人工降级路径。
- 上传与日志默认按敏感输入处理，执行类型/大小限制和信息裁剪。
- 新增依赖前检查用途、维护状态、许可证和安全影响。
- 同步更新 README、配置示例、测试和相关文档。

## 6. AI 功能实现准则

- 将问询、结构化、摘要、问答、案例整理拆成独立任务，避免使用拥有全部数据和权限的“万能 Agent”。
- 调用模型前按当前用户和任务裁剪输入；模型不能成为绕过字段权限的通道。
- 优先使用可校验的结构化输出；校验失败时进入草稿/人工处理。
- 在内部记录模型、提示模板版本、输入引用和审核结果，但日志不得复制不必要的敏感全文。
- UI 显示 AI 标识、生成时间、审核状态、来源范围和冲突/不确定信息。
- 为提示注入、知识库污染、无依据回答、权限泄露、错误确定性和服务失败建立测试。
- 模型置信度不能自动转换为线索可信等级。

## 7. 地图与定位实现准则

- 位置采集必须绑定具体任务、知情状态和明确的开始/停止时间。
- 根据角色提供不同字段和位置精度；默认不返回其他志愿者完整轨迹。
- 记录采集时间和精度，拒绝过期或异常位置。
- 提供地图服务不可用时的文字任务、静态点位或人工联络降级方案。
- 任务结束后停止采集，并按最短必要期限保留轨迹。
- 不由算法自动派遣人员进入山林、水域、铁路、高速、施工区等高风险区域。

## 8. 数据、示例和测试

- 默认使用固定、可重复的虚构数据。
- 电话使用明显的保留/无效示例格式，不生成可能误拨到真实个人的号码。
- 人像使用获得授权的资源或合规生成素材，并记录用途和来源。
- 地点可使用公共 POI，但不得拼接成影射真实案件的叙事。
- 测试至少覆盖正常流程、越权、缺失/冲突信息、外部服务失败和数据删除。
- 不以手工点击成功代替权限、安全和状态机测试。

## 9. 命令与验证

当前仓库没有构建系统，因此没有默认安装、格式化、测试或启动命令。代理不得虚构命令。

引入工程栈的首个变更必须在本节及 `README.md` 中补充：

```text
运行时版本：TBD
安装依赖：TBD
开发启动：TBD
格式检查：TBD
静态检查：TBD
单元测试：TBD
端到端测试：TBD
生产构建：TBD
模拟数据初始化：TBD
```

完成实现任务后，应运行与风险相称的验证并如实报告结果。若因环境、凭据或依赖无法运行，明确说明未验证内容，不能写成“应当可以”。

## 10. 文档与沟通

- 使用“已实现”“计划”“候选”“禁止”等明确状态词。
- 先报告结果，再说明关键改动、验证和剩余限制。
- 引用具体文件；不要用笼统描述掩盖未完成工作。
- 需求改变时同步检查产品、架构、数据、隐私、Demo、贡献和分发文档。
- 对真实搜救效果、合作单位、合规状态和 AI 准确率只引用可验证证据。

## 11. 完成标准

一项任务只有在以下条件满足时才可报告完成：

- 用户要求的文件或行为已经实际实现；
- 相关权限、失败路径和敏感数据影响已考虑；
- 已运行可用的检查/测试，结果已记录；
- 文档与实现状态一致；
- 没有把关键步骤留成未说明的 TODO；
- 未解决风险和外部依赖已在交付说明中明确列出。
